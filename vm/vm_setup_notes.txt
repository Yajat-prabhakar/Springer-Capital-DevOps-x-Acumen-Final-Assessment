VM SETUP NOTES (Extra Credit)
=============================

Goal: deploy a small VirtualBox VM and run the Docker/Nomad job from this
repository inside it, as an alternative to running everything directly in
WSL2.

1. VM creation
--------------

- Hypervisor: VirtualBox <version>
- Base image/ISO: <e.g. Ubuntu Server 24.04 LTS>
- VM specs: <CPU cores>, <RAM>, <disk size>
- Network mode: <NAT / Bridged / Host-only> - <chosen mode, and why>


2. Base provisioning inside the VM
-----------------------------------

Commands run after first boot to install Docker and Nomad:

    sudo apt update && sudo apt upgrade -y

    # Docker
    curl -fsSL https://get.docker.com -o get-docker.sh
    sudo sh get-docker.sh
    sudo usermod -aG docker $USER

    # Nomad
    sudo apt install -y wget gpg
    wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg
    echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
    sudo apt update && sudo apt install -y nomad


3. Getting the project into the VM
-----------------------------------

Chosen method:

- git clone https://github.com/<your-username>/devops-intern-final.git

Alternative methods:

- VirtualBox shared folder mounted at <path>
- scp the repository into the VM manually


4. Running the pipeline inside the VM
-------------------------------------

    cd devops-intern-final

    docker build -t hello-devops:v1 .

    docker run --rm hello-devops:v1

    nomad agent -dev &

    nomad job run nomad/hello.nomad

    nomad job status hello


5. Result
---------

Paste the actual output here once the pipeline has been successfully
executed inside the VM.

Example:

    Client Status = complete
    ...

    $ nomad alloc logs <alloc-id>
    Hello, DevOps!

